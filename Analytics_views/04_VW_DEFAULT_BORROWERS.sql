CREATE OR REPLACE VIEW ANALYTICS.VW_DEFAULT_BORROWERS AS SELECT B.FIRST_NAME,B.LAST_NAME,L.LOAN_ID,D.DAYS_PAST_DUE,D.RISK_LEVEL FROM CURATED.BORROWERS B JOIN CURATED.LOANS L ON B.BORROWER_ID=L.BORROWER_ID JOIN CURATED.DELINQUENCY D ON L.LOAN_ID=D.LOAN_ID WHERE D.DELINQUENCY_STATUS='DEFAULT';